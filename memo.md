# C++応用講座cmake

## 背景
* 使っているがわからない

## 1. Cmakeの基本
* install
```sudo apt install cmake```
* CMakeListはmakeファイルをつくるためのもの
    * -> ビルド・システム
main.cppとCMakeListは同じフォルダに入れる
* スクリプト・モード
    * bashやコマンドプロンプトのようなもの
